server {
    listen 8080;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass https://sfl.world/api/;
        proxy_ssl_server_name on;
        proxy_set_header Host sfl.world;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
